import { createStore } from 'vuex'
import board from '../default-board'

export default createStore({
    state: {
        board,
        Hallo: 'Hallo',
    },
    mutations: {},
    actions: {},
    modules: {},
})
